# AGENTS.md

## Cursor Cloud specific instructions

This repository (`FadisChen/CursorRemote`) is currently an empty/blank repository with only a `README.md`. There are no applications, services, dependencies, or build systems to run.

- **No package manager or lock file** exists — when code is added, the update script and this section should be revised accordingly.
- **No lint, test, or build commands** are configured yet.
- Future agents should re-evaluate this file once application code is committed.
